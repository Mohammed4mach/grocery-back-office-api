name: .NET

on:
  pull_request:
    types:
      - closed
    branches: [production]
  workflow_dispatch:

jobs:
  build_deploy:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Publish
      run: |
        dotnet publish grocery-back-office-api.fsproj\
          -c Release\
          -o ./publish\
          -r linux-x64\
          --self-contained true

    - name: Deploy
      env:
        PRIVATE_KEY: ${{ secrets.SERVER_PRIVATE_KEY  }}
        HOSTNAME: ${{ secrets.HOSTNAME  }}
        USER_NAME: ${{ secrets.USER_NAME  }}

      run: |
        ssh -o StrictHostKeyChecking=no -i private_key ${USER_NAME}@${HOST_NAME} '
        sudo supervisorctl stop grocery-back-office-api
        '

        echo "$PRIVATE_KEY" > private_key && chmod 600 private_key
        scp -o StrictHostKeyChecking=no -i private_key\
          ./publish/* ${USER_NAME}@${HOST_NAME}:/var/www/grocery-back-office-api/

        ssh -o StrictHostKeyChecking=no -i private_key ${USER_NAME}@${HOST_NAME} '
        sudo chown -R www-data:www-data /var/www/grocery-back-office-api
        sudo chmod -R 755 /var/www/grocery-back-office-api

        sudo supervisorctl reread
        sudo supervisorctl update
        sudo supervisorctl start grocery-back-office-api
        sudo systemctl restart nginx
        '

